/// <reference types="cypress" />

import loc from '../../support/locators'
import command  from '../../support/commands'

describe('Teste', () => { 

    beforeEach(() => {
        cy.Login()
    });

})